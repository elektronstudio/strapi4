'use strict';

/**
 * anthropologies-of-space service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::anthropologies-of-space.anthropologies-of-space');
